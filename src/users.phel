(ns pii.redactor.users-entry
  (:require phel.cli :as cli)
  (:require pii.redactor.users-cli :refer [app]))

;; Command-line entry point.
(defn run []
  (cli/run app (cli/argv (vec (cons "users" *argv*)))))

(when-not *build-mode*
  (php/exit (run)))
